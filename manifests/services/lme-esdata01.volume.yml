---
# esdata01 -- kind: volume. Exemplar for the volume kind.
# Fidelity target = quadlet/lme-esdata01.volume (cosmetic-only diffs: a trailing
# space after `[Unit]` and a doubled blank line in the empty [Service] section).
id: esdata01
name: lme-esdata01
kind: volume
enabled_when: [always]

volume_name: lme_esdata01
driver: local

dependencies:
  partof: [lme]
  after: [lme]
